"""Holds the built UI bundle (``dist/``), produced by ``lineage-ui``.

The directory is empty in a fresh checkout: run ``npm install && npm run build``
in ``lineage-ui/`` to populate it. The API serves it when present and stays
headless when not.
"""
